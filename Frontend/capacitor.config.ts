import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.splitwise',
  appName: 'SplitWise',
  webDir: 'dist/splitwise-frontend',
  bundledWebRuntime: false
};

export default config;
